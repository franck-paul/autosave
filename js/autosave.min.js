"use strict";$((()=>{if(dotclear.autosave=dotclear.getData("autosave"),$("#id").length)$("#entry-form").append('<input type="hidden" id="save" value="save" name="save">'),$("#entry-form").autosave({interval:Number(dotclear.autosave.delay),monitor:3e3,save(){const e=new Date,t=dotclear.autosave.msg.saved.replace("%s",e.toLocaleTimeString());if($("#content p.message").length>0)return void $("#content p.message").text(t);const a=$(document.createElement("p"));a.addClass("message"),a.text(t),$("#content h2").after(a)}});else{const e=dotclear.autosave.msg.waiting;if($("#content p.message").length>0)$("#content p.message").text(e);else{const t=$(document.createElement("p"));t.addClass("message"),t.text(e),$("#content h2").after(t)}}}));
